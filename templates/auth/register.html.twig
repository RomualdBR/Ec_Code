{% extends 'auth.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<!-- Page -->
	<div class="flex items-center justify-center grow bg-center bg-no-repeat page-bg">
		<div class="card max-w-[370px] w-full">
			{{error}}
			{{ form_errors(registerForm) }}
			{{ form_start(registerForm, {'attr': {'class': 'card-body flex flex-col gap-5 p-10', 'data-turbo' : 'false'}}) }}
			<div class="text-center mb-2.5">
				<h3 class="text-lg font-medium text-gray-900 leading-none mb-2.5">
					Inscription
				</h3>
				<div class="flex items-center justify-center">
					<span class="text-2sm text-gray-700 me-1.5">
						Vous avez déjà un compte ?
					</span>
					<a class="text-2sm link" href="{{ path('auth.login') }}">
						Me connecter
					</a>
				</div>
			</div>

			<div class="flex flex-col gap-1">
				<label class="form-label text-gray-900">Email</label>
				<div class="input">{{ form_widget(registerForm.email)}}</div>
			</div>
			<div class="flex flex-col gap-1">
				<label class="form-label font-normal text-gray-900">Mot de passe</label>
				<div class="input" data-toggle-password="true">
					<div>
						{{ form_row(registerForm.plainPassword, {
            placeholder: 'Password'
        }) }}
					</div>
					<button class="btn btn-icon" data-toggle-password-trigger="true" type="button">
						<i class="ki-filled ki-eye text-gray-500 toggle-password-active:hidden"></i>
						<i class="ki-filled ki-eye-slash text-gray-500 hidden toggle-password-active:block"></i>
					</button>
				</input>
			</div>
		</div>
		<label class="checkbox-group">
			{{ form_widget(registerForm.agreeTerms) }}
			<span class="checkbox-label">
				J'accepte les
				<a class="text-2sm link" href="#">GCU</a>
			</span>
		</label>
		<button class="btn btn-primary flex justify-center grow">Inscription</button>
		{{ form_end(registerForm) }}
	</div>
</div>
<!-- End of Page -->{% endblock %}
